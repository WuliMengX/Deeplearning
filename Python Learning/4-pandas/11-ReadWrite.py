"""Reading/Writing Data to Different Sources - Excel, JSON, SQL, Etc"""
""" 
    excel安装包：
    pip install xlwt openpyxl xlrd

    df = pd.read_excel(path,)
    df.to_excel(path)
    
    连接SQL DATABASE：
        pip install SLAlchemy

        from sqlalchemy import create_engine
        import 相应database名
"""